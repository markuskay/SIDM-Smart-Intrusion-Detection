# Dataset

The dataset used for training is a network intrusion dataset 
containing Ethereum transaction features and network traffic metrics.

Features include protocol type, flag, sent/received transactions, 
ether values, ERC20 transaction counts, and binary threat labels.

To replicate, load your dataset into the notebooks/ folder and 
run Adaji_Model.ipynb from the beginning.