# Models

The trained model files (rf_model.pkl, xgb_model.pkl, lstm_model.h5) 
are not included in this repository due to file size constraints.

To generate the models, run the training notebook:
notebooks/Adaji_Model.ipynb

Models will be saved to your local models/ folder automatically.